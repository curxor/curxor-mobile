import React, { useEffect, useRef, useState } from "react";
import { Text, View } from "react-native";

interface ExpenseDetailsProps {
  id: string;
}

const ExpenseDetails: React.FC<ExpenseDetailsProps> = ({ id }) => {
  return <View></View>;
};

export default ExpenseDetails;
